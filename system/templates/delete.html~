{% extends "templates/base.html" %}

{%block title%}Infotrack - Remoção de usuário {% endblock %}
{% block content  %}

<div class="main-form">
  <h1>Gerenciamento de sistemas</h1>
  <hr />
					<h2>Confirmar remoção de sistema</h2>
					<em> Tem certeza que deseja remover o sistema {{main_system}}? Todos os usuários de cada sistema removido também serão removidos no processo. </em>
					
					{% if childs  %}
					    <p>
					        
					        Removendo este sistema, você também estará deletando os seus sistemas filhos listados abaixo:
					        <ul>
					        {% for s in childs %}
					            <li>{{s}}</li>
					        {% endfor %}
					        </ul>
					    </p>
					    {% endif %}
					<div align="center" style="margin-top:10px;" id="actions">
            
            <form action='.' method='post'>{% csrf_token %}
                <a href="#" onClick="parentNode.submit();">Confirmar</a>
                <a onClick="history.back();">Cancelar</a>
            </form>
            </div>

{% endblock %}
			<tr class="ui-widget-content jqgrow ui-row-ltr">
				<td>
					<h2>Confirmar remoção:</h2>
					    <p> Tem certeza que deseja remover o sistema {{main_system}}? Todos os usuários de cada sistema removido também serão removidos no processo.</p>
					    
					    {% if childs  %}
					    <p>
					        
					        Removendo este sistema, você também estará deletando os seus sistemas filhos listados abaixo:
					        <ul>
					        {% for s in childs %}
					            <li>{{s}}</li>
					        {% endfor %}
					        </ul>
					    </p>
					    {% endif %}
					    
					    <form action='.' method='post'>{% csrf_token %}
                            <input type="submit" value="Remover" /> <input type="button" value="Cancelar" onClick="history.back();"/>
                        </form>
					    
					         
				</td>
			</tr>
		</tbody>
	</table>
    
</div>
{% endblock %}
